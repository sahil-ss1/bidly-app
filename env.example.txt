# ===========================================
# BIDLY ENVIRONMENT VARIABLES
# ===========================================
# Rename this file to .env and fill in your values

# ===========================================
# SERVER CONFIGURATION
# ===========================================
NODE_ENV=development
PORT=5000

# Frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:5173

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Option 1: Individual variables (local development)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=bidly_db

# Option 2: Connection URL (cloud deployment - PlanetScale, Railway, etc.)
# DATABASE_URL=mysql://user:password@host:port/database
# MYSQL_URL=mysql://user:password@host:port/database

# Enable SSL for cloud databases
# DB_SSL=true

# ===========================================
# AUTHENTICATION
# ===========================================
# Generate a strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ===========================================
# GOOGLE CLOUD (Optional - for AI features)
# ===========================================
# Google Gemini API Key (for AI summaries)
GEMINI_API_KEY=your-gemini-api-key

# Google Cloud Storage (for file uploads)
GCS_PROJECT_ID=your-gcs-project-id
GCS_BUCKET_NAME=your-bucket-name
GCS_KEY_FILE=path/to/service-account-key.json

# ===========================================
# FRONTEND VARIABLES (Vite)
# ===========================================
# API URL for production frontend (leave empty for same-origin)
VITE_API_URL=

